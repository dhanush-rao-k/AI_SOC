# AI SOC

Scaffold for an AI-assisted SOC log analysis project.

The existing `llm/` implementation remains the active entry path, while the
new folders provide the requested package layout for embeddings, vector store,
correlation, ingestion, API, agent, utilities, dashboard, tests, and docs.